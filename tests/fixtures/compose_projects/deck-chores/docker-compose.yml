version: '2'

services:
  officer:
    image: funkyfuture/deck-chores:dev
    environment:
      DEBUG: 'no'
      LOG_FORMAT: '{levelname}: {message}'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
